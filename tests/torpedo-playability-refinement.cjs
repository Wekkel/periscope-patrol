#!/usr/bin/env node
'use strict';
const fs=require('fs'),vm=require('vm'),path=require('path');
const root=path.resolve(__dirname,'..');
function assert(name,cond,detail){if(!cond){console.error('FAIL:',name,detail||'');process.exitCode=1;}else console.log('PASS:',name,detail||'');}
let seed=1;function rand(){seed=(1664525*seed+1013904223)>>>0;return seed/4294967296;}
const M=Object.create(Math);M.random=rand;
const base={console,Math:M,Date,JSON,performance:{now:()=>0},setTimeout:()=>0,clearTimeout:()=>{},requestAnimationFrame:()=>0,cancelAnimationFrame:()=>{},localStorage:{getItem:()=>null,setItem(){},removeItem(){}},audio:new Proxy({}, {get:()=>()=>{}}),Toast:{show(){},ok(){},warn(){},bad(){},auto(){},stop(){}},showBriefing(){},particles:{spawnExplosion(){},draw(){}},navigator:{deviceMemory:8},window:{devicePixelRatio:1},document:{},innerWidth:1280,innerHeight:800};base.globalThis=base;
const ctx=vm.createContext(base);
const load=['js/core/utilities.js','js/data/torpedo-data.js','js/data/campaign-data.js','js/navigation/route-geometry.js','js/simulation/collision/hull-geometry.js','js/simulation/weapons/tdc-math.js','js/simulation/surface-watch.js','js/core/state.js','js/simulation/ship-damage.js','js/core/command-bus.js','js/persistence/save-system.js','js/simulation/engine-core.js','js/simulation/harbor.js','js/simulation/weapons/torpedoes.js','js/simulation/ai/enemy-ai.js','js/simulation/ai/aircraft.js','js/simulation/weapons/deck-gun.js','js/simulation/weapons/aa-gun.js','js/simulation/radio-intel.js','js/simulation/sensors.js','js/simulation/sound-radar.js','js/simulation/weather-system.js','js/simulation/ai/asw-brain.js','js/simulation/ai/escort-asw.js','js/simulation/collision/vessel-collision.js','js/simulation/damage-control.js','js/simulation/career-history.js','js/simulation/physics-navigation.js','js/simulation/mission-framework.js'];
for(const f of load)vm.runInContext(fs.readFileSync(path.join(root,f),'utf8'),ctx,{filename:f});
vm.runInContext(fs.readFileSync(path.join(root,'js/controllers/touch-controller.js'),'utf8'),ctx,{filename:'js/controllers/touch-controller.js'});

const result=vm.runInContext(`(()=>{
 const out={};
 const mk=(source,posConf,conf=.95)=>{const s=createState('Solomon Sea');s.world.harbor=null;s.playerSub.position={xNm:0,yNm:0};s.playerSub.heading=0;s.playerSub.depthFeet=60;const tr={id:'M1',bearing:0,rangeEstimateNm:2,courseEstimate:90,speedEstimateKnots:10,confidence:conf,positionConfidence:posConf,positionSource:source,source,plotPosition:{xNm:0,yNm:-2},lastFixPosition:{xNm:0,yNm:-2},contactType:'MERCHANT',lengthYards:450};s.world.contactTracks={M1:tr};s.tdc.targetId='M1';s.tdc.autoTrack=true;const e=new SimEngine(s,{drain:()=>[]});e.updateTdc();return{s,e};};
 out.quality={visual:mk('VISUAL',.97,.98).s.tdc.solutionQuality,sj:mk('SJ RADAR',.78,.9).s.tdc.solutionQuality,hydro:mk('HYDROPHONE',.30,.95).s.tdc.solutionQuality};
 // Reproduce the old stale-TDC failure: target moves east for 60 s after designation.
 {const {s,e}=mk('VISUAL',.97,.98),tr=s.world.contactTracks.M1,tgt={id:'M1',name:'Merchant',type:'MERCHANT',displayType:'MERCHANT',position:{xNm:0,yNm:-1.5},heading:90,speedKnots:10,baseSpeed:10,lengthYards:450,visualProfile:1,acousticBase:.3,sunk:false};s.world.contacts=[tgt];Object.assign(tr,{bearing:0,rangeEstimateNm:1.5,plotPosition:{...tgt.position},lastFixPosition:{...tgt.position}});e.updateTdc();const oldGyro=s.tdc.gyroAngle;for(let k=0;k<60;k++){tgt.position.xNm+=knotsNmSec(10);tr.plotPosition={...tgt.position};tr.lastFixPosition={...tgt.position};tr.bearing=bearingBetween(s.playerSub.position,tgt.position);tr.rangeEstimateNm=distNm(s.playerSub.position,tgt.position);s.time.elapsedSeconds++;}s.tdc.dudMode='none';e.floodTube(1,false);e.fireTorpedo(1);const newGyro=s.tdc.gyroAngle;for(let i=0;i<1600&&!s.weapons.hits.length;i++){const dt=.1;tgt.position.xNm+=knotsNmSec(10)*dt;e.updateTorpedoes(dt);}out.liveTrack={oldGyro,newGyro,hit:s.weapons.hits.length>0,bearing:s.tdc.bearing};}
 // Each individual tube must aim at centre solution. No hidden permanent +/- degree bias.
 {const s=createState('Solomon Sea');out.tubeOffsets=s.weapons.tubes.map(t=>t.spreadOffsetDeg||0);}
 // MAP FIRE is fire-only: it must not auto-flood a dry tube. Multiple READY tubes remain queued one shot per tap.
 {const s=createState('Solomon Sea'),calls=[],toasts=[];Toast.warn=t=>toasts.push(['warn',t]);Toast.bad=t=>toasts.push(['bad',t]);const dispatch=c=>{calls.push(c);if(c.type==='FIRE_TORPEDO'){const t=s.weapons.tubes.find(x=>x.id===c.tubeId);if(t){t.status='EMPTY';t.flooded=false;}}};const fake={game:{getSnapshot:()=>s,dispatch}};TouchCtrl.prototype.quickFire.call(fake);out.quickDry={calls:[...calls],toasts:[...toasts],ready:s.weapons.tubes.filter(t=>t.status==='READY').length};calls.length=0;toasts.length=0;s.weapons.tubes[0].status='READY';s.weapons.tubes[0].flooded=true;s.weapons.tubes[1].status='READY';s.weapons.tubes[1].flooded=true;TouchCtrl.prototype.quickFire.call(fake);TouchCtrl.prototype.quickFire.call(fake);out.quickReady={calls:[...calls],toasts:[...toasts]};}
 // Bulk forward flooding must match its UI label and leave aft tubes dry.
 {const s=createState('Solomon Sea'),e=new SimEngine(s,{drain:()=>[]});e.applyCmd({type:'FLOOD_ALL_TUBES'});out.fwdFlood={fwd:s.weapons.tubes.filter(t=>t.pos==='FWD').map(t=>t.status),aft:s.weapons.tubes.filter(t=>t.pos==='AFT').map(t=>t.status)};}
 // A green visual solution should be rewarding but reduced duds still prevent certainty.
 {let detonations=0,duds=0,fired=0;for(let n=1;n<=200;n++){seed=n;const s=createState('Solomon Sea');s.world.harbor=null;s.playerSub.position={xNm:0,yNm:0};s.playerSub.heading=0;s.playerSub.depthFeet=60;s.tdc.dudMode='reduced';const tgt={id:'M1',name:'Merchant',type:'MERCHANT',displayType:'MERCHANT',position:{xNm:0,yNm:-1.5},heading:90,speedKnots:10,baseSpeed:10,lengthYards:450,visualProfile:1,acousticBase:.3,sunk:false};s.world.contacts=[tgt];const tr={id:'M1',bearing:0,rangeEstimateNm:1.5,courseEstimate:90,speedEstimateKnots:10,confidence:.98,positionConfidence:.97,positionSource:'VISUAL',source:'VISUAL',plotPosition:{...tgt.position},lastFixPosition:{...tgt.position},lastUpdated:0,staleSeconds:0,contactType:'MERCHANT',lengthYards:450,visualHullConfirmed:true};s.world.contactTracks={M1:tr};s.tdc.targetId='M1';s.tdc.autoTrack=true;const e=new SimEngine(s,{drain:()=>[]});e.updateTdc();e.floodTube(1,false);e.fireTorpedo(1);if(s.weapons.activeTorpedoes.length)fired++;for(let i=0;i<1600&&!s.weapons.hits.length&&!s.weapons.duds.length;i++){const dt=.1;tgt.position.xNm+=knotsNmSec(10)*dt;e.updateTorpedoes(dt);}detonations+=s.weapons.hits.length>0;duds+=s.weapons.duds.length>0;}out.goodShot={fired,detonations,duds,rate:detonations/fired};}
 return out;
})()`,ctx);

const mapSrc=fs.readFileSync(path.join(root,'js/rendering/map.js'),'utf8');
const labelBlock=mapSrc.slice(mapSrc.indexOf('// Keep a busy convoy'),mapSrc.indexOf('  /* ── Top-down ship icons'));
const html=fs.readFileSync(path.join(root,'index.html'),'utf8');
assert('selected MAP label has no opaque black text card',!labelBlock.includes('fillRect(')&&!labelBlock.includes("rgba(3,13,16"),{});
assert('selected MAP label uses normal yellow chart ink',labelBlock.includes('rgba(245,198,92'),{});
assert('reduced dud mode is the default while historical remains available',vm.runInContext("createState('Solomon Sea').tdc.dudMode",ctx)==='reduced'&&/value="historical"/.test(html)&&/value="reduced" selected/.test(html),{});
assert('solution quality reflects positional knowledge: visual > SJ > hydrophone',result.quality.visual>.85&&result.quality.sj>.65&&result.quality.hydro<.35,result.quality);
assert('live selected TDC track updates before fire and fixes the 60 s stale-solution miss',result.liveTrack.hit&&result.liveTrack.newGyro-result.liveTrack.oldGyro>5&&result.liveTrack.bearing>5,result.liveTrack);
assert('individual tubes no longer carry permanent spread bias',result.tubeOffsets.every(x=>x===0),result.tubeOffsets);
assert('MAP FIRE with dry tubes warns and does not auto-flood or dispatch',result.quickDry.calls.length===0&&result.quickDry.ready===0&&result.quickDry.toasts.some(x=>x[0]==='warn'&&/ATTACK/.test(x[1])&&/flood/i.test(x[1])),result.quickDry);
assert('MAP FIRE consumes one already-ready tube per click without flooding another',result.quickReady.calls.length===2&&result.quickReady.calls.every(x=>x.type==='FIRE_TORPEDO')&&result.quickReady.calls[0].tubeId===1&&result.quickReady.calls[1].tubeId===2,result.quickReady);
assert('Flood All Fwd control floods forward tubes only',result.fwdFlood.fwd.every(x=>x==='READY')&&result.fwdFlood.aft.every(x=>x==='LOADED_DRY'),result.fwdFlood);
assert('good visual 1.5 nm shot is rewarding but not guaranteed under reduced duds',result.goodShot.rate>=.75&&result.goodShot.rate<=.95&&result.goodShot.duds>=10&&result.goodShot.duds<=50,result.goodShot);
if(process.exitCode)process.exit(process.exitCode);console.log('TORPEDO PLAYABILITY REFINEMENT: PASS');
